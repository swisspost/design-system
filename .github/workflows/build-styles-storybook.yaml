###
#
# Build the styles storybook
#
###

name: Build Styles Storybook
on:
  pull_request:
    # paths:
    #   - 'packages/styles/**/*.stories.js'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: swisspost/design-system/.github/actions/setup.environment.pnpm
