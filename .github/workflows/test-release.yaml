name: Test workflow
on: push

jobs:
  success:
    steps:
      - name: good
        run: exit 0
  fail:
    steps:
      - name: fail
        run: exit 1

  test2:
    runs-on: ubuntu-latest
    if: always() && needs.success.result == "success"
    needs: [success, fail]
    steps:
      - name: Always step
        run: echo "Always!"
