import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import meta, * as Skiplinks from './skiplinks.stories';
import StylesPackageImport from '@/shared/styles-package-import';
import PackageTag from '@/shared/package-tag';

<Meta of={Skiplinks} />

<div className="docs-title">
  # Skiplinks
</div>

<PackageTag meta={meta} />

<div className="lead">
  Skip links let people using a keyboard, screen reader, switch control, or other assistive
  technology reach main content or other important sections faster and easier.
</div>

For the skiplinks to work, don't forget to add corresponding ID to the element that needs to be skipped to.

<Canvas sourceState="shown" of={Skiplinks.Default} />

<StylesPackageImport components={['skiplinks']} />
