import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as linkareaStories from './linkarea.stories';
import StoryTags from '@/shared/story-tags';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={linkareaStories} />

# Link Area

<StoryTags meta={meta} />

<div className="lead">
  The link area component lets you make an entire block of content clickable through a single
  interactive element.
</div>

The `<post-linkarea>` component allows only one interactive element to avoid user confusion over which link would be activated on click.

<Canvas sourceState="shown" of={linkareaStories.Default} />
<div className="hide-col-default">
  <Controls of={linkareaStories.Default} />
</div>

<ComponentsPackageInstall component="<post-linkarea>" />
