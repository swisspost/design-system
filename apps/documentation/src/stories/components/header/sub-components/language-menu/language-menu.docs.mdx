import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as languageMenuStories from './language-menu.stories';
import * as languageMenuItemStories from '../language-menu-item/language-menu-item.stories';
import { PostBanner } from '@swisspost/design-system-components-react';
import StoryTags from '@/shared/story-tags';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={languageMenuStories} />

<div className="docs-title">
  # Language Menu

<link-design of={JSON.stringify(languageMenuStories)}></link-design>

</div>

<StoryTags meta={meta} />

<p className="lead">The language menu enables users to select their preferred language.</p>

It is designed to be used within the `post-header` component.

<Canvas sourceState="shown" of={languageMenuStories.Default} />

<Controls of={languageMenuStories.Default} />

<PostBanner>
  <p>
    <b>Accessibility Note:</b> For each language in the selection menu, the accessible name is
    generated using the `name` property. It is essential that this property is set with the full,
    readable name of the language (e.g., English) to serve as an accurate and clear accessible name
    for assistive technologies.
  </p>
</PostBanner>

<ComponentsPackageInstall component="<post-language-menu>" />

## Language menu item

The `post-language-menu` consists of a list of `post-language-menu-item`.

<Canvas sourceState="shown" of={languageMenuItemStories.Default} />
<Controls of={languageMenuItemStories.Default} />

## Links

The language menu items can be used as links, which will navigate to a different page upon selection.

<Canvas sourceState="shown" of={languageMenuStories.Links} />
