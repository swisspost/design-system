import ComponentsPackageInstall from '@/shared/components-package-install.mdx';
import StoryTags from '@/shared/story-tags';
import StylesPackageImport from '@/shared/styles-package-import';
import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks';
import LandmarkCorrect from '../../../shared/samples/landmark-placement-example.sample.html?raw';
import meta, * as HeaderStories from './header.stories';
import './header.styles.scss';
import SampleSessionSubscription from './session-subscription.sample?raw';

<Meta of={HeaderStories} />

<div className="docs-title">
  # Header

<link-design of={JSON.stringify(HeaderStories)}></link-design>

</div>

<StoryTags meta={meta} />

<div className="lead">
  The Header is a consistently available interface element that provides a consistent navigation
  experience across the digital Post systems, products and services.
</div>

<post-tabs size="large">
  <post-tab-item name="overview">Overview</post-tab-item>
  <post-tab-item name="portal">Post Portal</post-tab-item>
  <post-tab-item name="microsite">Microsite</post-tab-item>
  <post-tab-item name="one-pager">One Pager</post-tab-item>
  <post-tab-item name="application">Application</post-tab-item>
<post-tab-panel for="overview" class="pt-24">
    
Allow users to navigate through your application.

It adapts to your needs and can contain a variety of sub-components depending on your use case.

The Header is divided into two sections:

1. **Global navigation** (Yellow Background): provides navigation links and information that are global to the entire organization.
2. **Local navigation** (White Background): displays navigation links and information specific to the current application.

<div className="main-story">
  <Canvas sourceState="shown" of={HeaderStories.Default} />
</div>
<div className="hide-col-default">
  <Controls of={HeaderStories.Default} />
</div>

</post-tab-panel>
<post-tab-panel for="portal" class="pt-24">

The Header for the Post Portal (post.ch) is a consistently available interface element that provides a consistent navigation experience across the whole Post Portal and other portals.

It not only contains globally available key elements (such as Language), it also provides a first content selection via the audience buttons. They define the further navigation.

<Canvas of={HeaderStories.Portal} inline="false" />

## Jobs

A dedicated header for the Jobs pages within the Post Portal.

<Canvas of={HeaderStories.Jobs} inline="false" />

## User menu

The `post-login` slot in `<post-header>` holds either a login link when no user is authenticated, or the user menu when a user is authenticated.

The authentication mechanism and conditional display are handled by each project individually.
The Design System provides the UI components, while the application manages the authentication logic and decides which content to render in the `post-login` slot.

### Fetching user data

One method to retrieve the current Post user session data is via the session subscription endpoint.
The URL shown below is for the **production** environment, adapt it to match your target environment (e.g. int, test).

<Source code={SampleSessionSubscription} language="typescript" />

If the request fails or returns no valid `userdata`, the user is not authenticated and the login link can be displayed instead.

### Logged out

When no user is authenticated, a login link is slotted into `post-login`:

<Canvas of={HeaderStories.LoggedOut} />

### Logged in

When a user is authenticated, the user menu is slotted into `post-login`. The session data can be used to populate the avatar and menu header:

<Canvas of={HeaderStories.LoggedIn} />

</post-tab-panel>

<post-tab-panel for="microsite" class="pt-24">

The Microsite Header is a consistently available interface element that provides a consistent navigation experience across the whole Microsite.

<Canvas of={HeaderStories.Microsite} inline="false" />

</post-tab-panel>

<post-tab-panel for="one-pager" class="pt-24">

The One Pager Header is a consistently available interface element that provides a consistent navigation experience across the whole One Pager.

<Canvas of={HeaderStories.OnePager} inline="false" />

You can use the title inside the `post-header` section as the main title for your webpage.
To do this, wrap the title text in an `<h1>` element.

A page should generally have **only one `<h1>` element**,
this helps with accessibility and search engine optimization.
Use other heading levels (`<h2>`–`<h6>`) for subsections of your content.

For more information about heading tags and their usage,
see the [Heading elements documentation](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/Heading_Elements) on MDN.

<Canvas of={HeaderStories.OnePagerH1} inline="false" />

</post-tab-panel>

<post-tab-panel for="application" class="pt-24">

The Application header is a header variant intended for internal applications. It uses the [compact styles bundle](/?path=/docs/e53e2de8-0bbf-4f70-babc-074c5466f700--docs&devModeEnabled=true#available-bundles) and is distinguished by the absence of any content in its global (yellow) part except from the Logo. For applications that require in-app navigation, the Application header supports [Side Navigation](/?path=/docs/9f26d86e-7edb-5804-ac96-92g22f91c9d9--docs).

<Canvas of={HeaderStories.Application} inline="false" />

## Example with Side Navigation

The Application header can be paired with a [Side Navigation](/?path=/docs/9f26d86e-7edb-5804-ac96-92g22f91c9d9--docs) to provide hierarchical navigation within the application.

<Canvas of={HeaderStories.ApplicationWithSideNavigation} inline="false" />

</post-tab-panel>
</post-tabs>

<ComponentsPackageInstall component="<post-header>" />

<StylesPackageImport components={['header', 'list', 'button']} />

## CSS Properties

The `<post-header>` styles expose several custom properties on the `:root` element:

- `--post-header-height`: the current visible height of the header
- `--post-header-expanded-height`: the height of the header when expanded
- `--post-header-reduced-height`: the height of the header when reduced

In addition, the `<post-header>` styles set the `scroll-padding-top` property on the `<body>` element to match the currently visible header height.
This ensures that when using scroll navigation (e.g. jumping to anchors), content is positioned correctly below the header.

## Active Navigation Items

**The `aria-current="page"` attribute highlights the active navigation items**

This attribute is not added automatically,
it must be applied by your application logic to the `<a>` element of the `<post-mainnavigation>` that represents the page currently being viewed.
Once applied, parent items in the navigation automatically inherit the correct selected styling.

<Canvas of={HeaderStories.ActiveNavigationItem} />

## Accessibility

### Banner Landmark

The header provides a **banner landmark** that identifies site-oriented content at the beginning of each page. Screen reader users can jump directly to the banner using landmark navigation shortcuts.
Each page must have **exactly one banner landmark** at the top level:

<Source code={LandmarkCorrect} language="html" />

### Navigation Landmark

The `<post-mainnavigation>` component inside the header creates a **navigation landmark**.
Use the `text-main` property to provide a descriptive label that helps screen reader users identify main navigation among other navigation regions on the page.

**Why labeling matters:** If your page has multiple navigation regions (e.g., skip links, main navigation, breadcrumbs), descriptive labels help screen reader users quickly identify and navigate to the correct one.

For more information, see the [WAI-ARIA Banner Landmark documentation](https://www.w3.org/WAI/ARIA/apg/patterns/landmarks/examples/banner.html) and [WAI-ARIA Navigation Landmark documentation](https://www.w3.org/WAI/ARIA/apg/patterns/landmarks/examples/navigation.html).
