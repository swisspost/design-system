import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as ListGroup from './list-interactive.stories';
import StylesPackageImport from '@/shared/styles-package-import';
import PackageTag from '@/shared/package-tag';

<Meta of={ListGroup} />

<div className="docs-title">
  # List Interactive

<link-design of={JSON.stringify(ListGroup)}></link-design>

</div>

<PackageTag meta={meta} />

<div className="lead">
  The list interactive groups related, clickable items (such as links, documents, or switches)
  organized vertically in a single list.
</div>

<Canvas sourceState="shown" of={ListGroup.Default} />
<div className="hide-col-default">
  <Controls of={ListGroup.Default} />
</div>

<StylesPackageImport components={['list-interactive']} />

## Types of lists

### List Links

The links list collects and displays a set of links. It is therefore a navigational element which leads the user to other locations.

<Canvas of={ListGroup.ListLinks} />

### List Documents

The documents list collects and displays a set of documents. It does not only help users find a specific item, but also provides the user with a button to download the respective document.

<Canvas of={ListGroup.ListDocuments} />

### List Switch

The switches list collects and displays a set of switches. It does not only help users find a specific item, but also lets them toggle it on or off directly within the list, with the same interaction whether selecting or deselecting.

<Canvas of={ListGroup.ListSwitch} />