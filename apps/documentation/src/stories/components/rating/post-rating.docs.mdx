import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks';
import meta, * as RatingStories from './post-rating.stories';
import SampleEvents from './post-rating.events.sample?raw';
import PackageTag from '@/shared/package-tag';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={RatingStories} />

<div className="docs-title">
  # Rating

<link-design of={JSON.stringify(RatingStories)}></link-design>

</div>

<PackageTag meta={meta} />

<p className="lead">
  The rating allows users to provide feedback by selecting a visual star rating, reflecting their
  satisfaction level or quality assessment.
</p>

<Canvas of={RatingStories.Default} sourceState="shown" />
<Controls of={RatingStories.Default} />

<ComponentsPackageInstall component="<post-rating>" />

## Examples

### Events

<Source code={SampleEvents} language="ts" />
