import { Meta, Source, Canvas } from '@storybook/addon-docs/blocks';
import meta, * as BackToTopStories from './back-to-top.stories';
import BackToTop from './back-to-top.sample.html?raw';
import StoryTags from '@/shared/story-tags';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={BackToTopStories} />

<div className="docs-title">
 # Back To Top

<link-design of={JSON.stringify(BackToTopStories)}></link-design>

</div>

<StoryTags meta={meta} />

<div className="lead">
  The Back to top component enables the user to scroll back to the top of the page with only a
  click. This is especially helpful for pages which require a lot of scrolling to reach the end.
</div>

<Source code={BackToTop} language="html" />

<post-banner>
  The `textBackToTop` property is essential for accessibility and must provide a clear, descriptive
  text for the Back to Top button.
</post-banner>

<ComponentsPackageInstall component="<post-back-to-top>" />

## Examples

See inlined story below, scroll down to see the `Back to Top` button appear in the bottom right corner of the page. On click it will scroll back to top.

<Canvas of={BackToTopStories.Default} />
