import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as SideNavigationStories from './side-navigation.stories';
import PackageTag from '@/shared/package-tag';
import StylesPackageImport from '@/shared/styles-package-import';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={SideNavigationStories} />

<div className="docs-title">
  # Side Navigation
  <link-design of={JSON.stringify(SideNavigationStories)}></link-design>
</div>

<PackageTag meta={meta} />

<div className="lead">
  The side navigation is a container of navigation menu items that let the user navigate through different parts of a web application. It is a hierarchical structure that provides a maximum of 4 nested navigation levels which can be expanded or collapsed.
</div>

<Canvas sourceState="shown" of={SideNavigationStories.Default} />
<div className="hide-col-default">
  <Controls of={SideNavigationStories.Default} />
</div>

<ComponentsPackageInstall component="<post-side-navigation>" />

<StylesPackageImport components={['side-navigation', 'list']} />

## Navigation content

The navigation content is authored by you and projected through the default slot. It must be a `<nav>` landmark, with a heading (`<h*>`, or a `<p>` if no heading level fits) linked via `aria-labelledby`, and `<ul>`/`<li>` lists for the items. Nested levels are nested `<ul>`s.

### Icon usage

Icons help users scan and recognize navigation categories using simple, meaningful icons with labels.

**When using icons**:

- **Level-1 only**: Never on deeper levels (2, 3, or 4)
- **All or none**: Apply to all level-1 items or none, do not mix

## Concrete examples of application

### Link only

Links that navigate to different pages without disclosure.

<Canvas sourceState="hidden" of={SideNavigationStories.LinkOnly} />

### Nested but not collapsible

Nested children are always visible. The parent can be a link or a non-clickable `<span>`. Use this when you want to show all levels at once without hiding any sections.

<Canvas sourceState="hidden" of={SideNavigationStories.Nested} />

### Collapsible but not linked

Use a button inside `<post-collapsible-trigger>` to toggle the visibility of child items. The collapsible element is a direct child of the trigger. Use this when the level-1 item should only disclose children, not navigate anywhere.

<Canvas sourceState="hidden" of={SideNavigationStories.CollapsibleNotLinked} />

### Collapsible and linked

Both navigate and expand: the link takes you to a page, and the separate button toggles visibility of child items. The button carries a visually hidden label for accessibility. The collapsible is a direct child of the trigger.

<Canvas sourceState="hidden" of={SideNavigationStories.CollapsibleLinked} />

### Active item

Mark the link of the current page with `aria-current="page"`. The component highlights it as the active item, and screen reader users hear it announced as the current page. Set it from your routing, not on click.

<Canvas sourceState="hidden" of={SideNavigationStories.ActiveNavigationItem} />

## Accessibility

### Navigation landmark

The navigation you project is a `<nav>` landmark. Link it to its heading with `aria-labelledby` (see the examples above) so screen reader users can identify it and jump to it with landmark shortcuts.

**Why labeling matters:** if a page has several navigation regions (main navigation, breadcrumbs, this side navigation), a descriptive heading lets screen reader users tell them apart and move to the right one.

### Links and disclosure buttons

Use `<a>` for links and a `<button>` inside `<post-collapsible-trigger>` for collapsible sections. A link navigates, a button discloses; never nest one inside the other.

### Focus management

On mobile, focus moves into the navigation when the dialog opens and returns to the trigger button when it closes. The rest of the page is inert while the dialog is open.