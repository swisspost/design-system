import { Meta, StoryObj } from '@storybook/web-components-vite';
const meta: Meta = {
  title: 'Health',
  tags: ['devOnly'],
};

export default meta;

type Story = StoryObj;

export const Health: Story = {};
