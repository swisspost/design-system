import { Meta, StoryObj } from '@storybook/web-components-vite';

const meta: Meta = {
  title: 'Health/Package Stats',
  tags: ['devOnly'],
};

export default meta;

type Story = StoryObj;

export const Default: Story = {};
