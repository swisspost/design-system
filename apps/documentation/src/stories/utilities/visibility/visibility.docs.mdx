import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import * as VisibilityStories from './visibility.stories';

<Meta of={VisibilityStories} />

# Visibility

<div className="lead">
  Use our visibility utilities to control if elements are visible or hidden. Elements with the{' '}
  <code>.invisible</code> class are hidden, but they still take up space on the page.
</div>

**Available classes :**

- <code>visible</code>
- <code>invisible</code>

## Example

<Canvas sourceState="shown" of={VisibilityStories.Default} />
<div className="hide-col-default">
  <Controls of={VisibilityStories.Default} />
</div>
