# Swiss Post Design System - Angular Integration

Angular integration test app for the Swiss Post Design System.

## Development

Run `pnpm start` to start the development server on port 9210.

## Build

Run `pnpm build` to build the app. The build artifacts will be stored in the `dist/` directory.

## Testing

Run `pnpm e2e` to execute end-to-end tests with Playwright.
