{/* eslint-disable no-undef */}
<div className="banner banner-warning mb-40">
  <h2 className="banner-heading">The {props.component} component is part of the legacy version of our design system built using ng-bootstrap, which has been removed as a dependency in this new version.</h2>
  <p>This is its old documentation and is not accurate. A new version of this component as well as a new documentation will be available soon.</p>
</div>
<div className="banner banner-info mb-40">
  <h2 className="banner-heading">The {props.component} is an ng-bootstrap component.</h2>
  <p>This documentation gives only the specifics for a usage within Post project.<br/>
    Read the complete information about the component's configuration in <a href={`https://ng-bootstrap.github.io/#/components/${props.component}`} rel="nofollow noopener noreferrer">ng-bootstrap {props.component} documentation</a>.</p>
</div>
{/* eslint-enable no-undef */}
