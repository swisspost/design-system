<div className="alert alert-info mb-40">
  <p className="alert-heading">The {props.component} is an ng-bootstrap component.</p>
  <p>This documentation gives only the specifics for a usage within Post project.<br/>
    Read the complete information about the component's configuration in <a href={`https://ng-bootstrap.github.io/#/components/${props.component}`} rel="nofollow noopener noreferrer">ng-bootstrap {props.component} documentation</a>.</p>
</div>
