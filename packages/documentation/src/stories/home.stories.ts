import { Meta, StoryObj } from '@storybook/web-components';

const meta: Meta = {
  id: 'Home',
  title: 'Home',
  parameters: {
    badges: [],
  },
};

export default meta;

type Story = StoryObj;

export const Default: Story = {};
