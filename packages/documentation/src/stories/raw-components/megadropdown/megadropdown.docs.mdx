import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as megadropdownStories from './megadropdown.stories';
import PackageTag from '@/shared/package-tag';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={megadropdownStories} />

<div className="docs-title">
  # Mega dropdown

  <link-design of={JSON.stringify(megadropdownStories)}></link-design>
</div>

<PackageTag meta={meta}/>

<p className="lead">
  The megadropdown is used within the `post-header` main navigation to display submenus.
</p>

<Canvas sourceState="shown" of={megadropdownStories.Default} />

<Controls of={megadropdownStories.Default} />

<ComponentsPackageInstall component="<post-megadropdown>" />
