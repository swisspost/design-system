import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import * as languageOptionStories from './language-option.stories';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={languageOptionStories} />

<div className="docs-title">
  # Language Option

  <link-design of={JSON.stringify(languageOptionStories)}></link-design>
</div>

<p className="lead">Enable users to select their preferred language.</p>

<Canvas sourceState="shown" of={languageOptionStories.Default} />

<Controls of={languageOptionStories.Default} />

<ComponentsPackageInstall component="<post-language-option>" />

## Examples

### Button

Use the `onClick` event to change the language of your page programmatically whenever the user clicks on the `post-language-option`.

<Canvas sourceState="shown" of={languageOptionStories.Default} />

### Link

Provide a `url` property in the `post-language-option` component to redirect the user to another page.

<Canvas sourceState="shown" of={languageOptionStories.Anchor} />
