import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import * as FooterStories from './footer.stories';

<Meta of={FooterStories} />

<div className="docs-title">
  # Footer

  <link-design of={JSON.stringify(FooterStories)}></link-design>

</div>

<div className="lead">
  The footer can be found at the bottom of almost every web page. It provides the user with important information and links (contextual and across Post), but also contains critical elements from a legal perspective.
</div>

<post-banner class="mt-24 mb-36">
  Currently, the footer may only be used in addition to the internet-header.

  For more information, read the <a href="/?path=/docs/cfa21cbc-5f8d-4a42-8165-7f6ed20bf133--docs">documentation about the internet header package</a>.
</post-banner>

The footer comes with its own container and can be placed at the end of your content.

<Canvas sourceState="shown" of={FooterStories.Default} />

<Controls of={FooterStories.Default} />
