import { Meta, StoryObj } from '@storybook/web-components-vite';

const meta: Meta = {
  title: 'Health/Icons',
  tags: ['devOnly'],
};

export default meta;

type Story = StoryObj;

export const Report: Story = {};
