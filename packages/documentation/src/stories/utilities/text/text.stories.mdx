import { Meta, Canvas, Story } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { SCSS_VARIABLES, AvailableClassesAndVariables } from './text.components';

<Meta
  title="Utilities/Text"
  parameters={{
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
    viewMode: 'docs',
    badges: [BADGE.NEEDS_REVISION],
  }}
/>

# Text

<p className="lead">
  Documentation and examples for common text utilities to control font family, size, alignment,
  wrapping, and more.
</p>

## Font

Quickly change how fonts are displayed in specific elements and/or their child nodes.<br/>
To do so, just add the class to your element or use the scss-variable in your code.

### Font Family

<Canvas withSource="open">
  <Story name="Font Family">
    <p className="font-sans-serif">Sans Serif text, ...</p>
  </Story>
</Canvas>

#### Set Font Family in scss

```css dark
@use '@swisspost/design-system-styles/core' as post;

.my-element {
  font-family: post.$font-family-sans-serif;
}
```

#### Available values

<AvailableClassesAndVariables
  variants={SCSS_VARIABLES.fontFamilies}
  cssPrefix="font"
  scssPrefix="font-family"
/>

### Font Size

While our heading classes (`.h1` –`.h6`) apply`font-size`, `font-weight`, and`line-height`, these utility classes only apply the `font-size` to an element.

<Canvas withSource="open">
  <Story name="Font Size">
    <p className="fs-tiny">Tiny text, ...</p>
  </Story>
</Canvas>

#### Set Font Size in scss

```css dark
@use '@swisspost/design-system-styles/core' as post;

.my-element {
  font-size: post.$font-size-tiny;
}
```

#### Available values

<AvailableClassesAndVariables
  variants={SCSS_VARIABLES.fontSizes}
  cssPrefix="fs"
  scssPrefix="font-size"
/>

### Font Weight

<div class="alert alert-warning my-4">
  <p>
    The classes <code>bold</code>, <code>regular</code> and <code>light</code> are being deprecated
    in favor of the bootstrap naming.
  </p>
</div>

<Canvas withSource="open">
  <Story name="Font Weight">
    <p className="fw-bold">Bold text, ...</p>
  </Story>
</Canvas>

#### Set Font Weight in scss

```css dark
@use '@swisspost/design-system-styles/core' as post;

.my-element {
  font-weight: post.$font-weight-bold;
}
```

#### Available values

<AvailableClassesAndVariables
  variants={SCSS_VARIABLES.fontWeights}
  cssPrefix="fw"
  scssPrefix="font-weight"
/>

### Font Style

<Canvas withSource="open">
  <Story name="Font Style">
    <p className="fst-italic">Italic text, ...</p>
  </Story>
</Canvas>

#### Set Font Style in scss

```css dark
@use '@swisspost/design-system-styles/core' as post;

.my-element {
  font-style: post.$font-style-italic;
}
```

#### Available values

<AvailableClassesAndVariables
  variants={SCSS_VARIABLES.fontStyles}
  cssPrefix="fst"
  scssSlot="There are no variables available for font-styles at the moment."
/>

### Line Heights

<div class="alert alert-warning my-4">
  <p>
    The line-height variants are being deprecated in favour of more accessible line heights of{' '}
    <code>1.5</code> for body text and <code>1.2</code> for headings. Bootstrap line-height variants
    are still available.
  </p>
</div>

<Canvas withSource="open">
  <Story name="Line Heights">
    <p className="lh-tiny">Text with a tiny line-height, ...</p>
  </Story>
</Canvas>

#### Set Line Height in scss

```css dark
@use '@swisspost/design-system-styles/core' as post;

.my-element {
  line-height: post.$line-height-tiny;
}
```

#### Available values

<AvailableClassesAndVariables
  variants={SCSS_VARIABLES.fontLineheights}
  cssPrefix="lh"
  scssPrefix="line-height"
/>

#### Bootstrap line heights

To set the `line-height` to 1 you can use the class `.lh-1`. Bootstrap variables are not exposed in the <code>@swisspost/design-system-styles/core.scss</code> file.

<AvailableClassesAndVariables
  variants={SCSS_VARIABLES.bootstrapLineHeights}
  cssPrefix="lh"
  hasVariable={false}
/>

## Text

Define how text-blocks are displayed.

### Color

<div class="alert alert-warning my-4">
  <p>
    The classe <code>text-auto</code> is being deprecated because it is no longer necessary in
    combination with our <LinkTo href="utilities-background">background utilities</LinkTo>.
  </p>
</div>

Colorize text with color utility classes. If you want to colorize links, you can use the `.link-*` classes instead, which comes with styles for `:hover` and `:focus` states.

<Canvas withSource="open">
  <Story
    name="Color"
    decorators={[
      Story => (
        <div onClick={e => e.preventDefault()}>
          <Story />
        </div>
      ),
    ]}
  >
    <p className="text-success">
      Colored text, ...
      <br />
      <a href="#" className="link-danger">
        Colored link, ...
      </a>
    </p>
  </Story>
</Canvas>

#### Set the Color in scss

```css dark
@use '@swisspost/design-system-styles/core' as post;

.my-element {
  color: post.$success;
}
```

#### Available values

### Reset Color

Reset a text color with `.text-reset`, so that an element inherits the color from its parent.

<Canvas withSource="open">
  <Story
    name="Reset Color"
    decorators={[
      Story => (
        <div onClick={e => e.preventDefault()}>
          <Story />
        </div>
      ),
    ]}
  >
    <p className="text-success">
      Colored text and a{' '}
      <a href="#" className="text-reset">
        link
      </a>{' '}
      with the same color.
    </p>
  </Story>
</Canvas>

### Alignment

Easily realign text with our text alignment utility classes.

<Canvas withSource="open">
  <Story name="Alignment">
    <p className="text-start">Start aligned text</p>
    <p className="text-center">Center aligned text</p>
    <p className="text-end">End aligned text</p>
  </Story>
</Canvas>

#### Responsive alignment

For `start`, `end`, and `center` alignment, responsive classes are available that use the same viewport breakpoints as the grid system.
The responsive alignment classes have the following format: `text-{breakpoint}-{alighment}`.

For more detailed informations about breakpoints read our <LinkTo kind="foundations-grid">grid docs</LinkTo>.

### Wrapping

Manage whether text should wrap or not with the utility classes `.text-nowrap` and `.text-wrap`.

<Canvas withSource="open">
  <Story name="Wrapping">
    <div className="w-50 bg-info">
      <p className=" text-nowrap">
        This text should NOT wrap, saepe excepturi quas nihil repudiandae eius assumenda voluptatem.
      </p>
    </div>
    <div className="w-50 bg-info text-nowrap">
      <p className="text-wrap">
        This text should wrap, saepe excepturi quas nihil repudiandae eius assumenda voluptatem.
      </p>
    </div>
  </Story>
</Canvas>

### Word Break

Prevent long strings of text from breaking your component's layout by using the utility class `.text-break`.

<Canvas withSource="open">
  <Story name="Word Break">
    <div className="w-50">
      <p className="text-break">wwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwww</p>
    </div>
  </Story>
</Canvas>

### Transform

Change the `text-transform` manually with the following utility classes: `.text-lowercase`, `.text-uppercase` and `.text-capitalize`.

<Canvas withSource="open">
  <Story name="Transform">
    <p className="text-lowercase">LOWERCASE TEXT, ...</p>
    <p className="text-uppercase">uppercase text, ...</p>
    <p className="text-capitalize">uppercase text, ...</p>
  </Story>
</Canvas>

### Decorations

Change the `text-decoration` manually with the following utility classes: `.text-decoration-line-through` and `.text-decoration-none`.

<Canvas withSource="open">
  <Story name="Decorations">
    <p className="text-decoration-line-through">This text has a line going through it.</p>
    <p>
      <a className="text-decoration-none">Link without decoration</a>
    </p>
  </Story>
</Canvas>
