import { Meta, StoryObj } from '@storybook/web-components';

const meta: Meta = {
  id: 'Changelog',
  title: 'Misc/Changelog',
};

export default meta;

type Story = StoryObj;

export const Default: Story = {};
