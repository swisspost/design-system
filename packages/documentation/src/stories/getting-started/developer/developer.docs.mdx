import { Meta } from '@storybook/blocks';
import * as DeveloperStories from './developer.stories';
import { packages } from '@/shared/packages.data';
import '../../../shared/tile/tile.component';
import { PackageType } from '@/../types';
import './list-component';

<Meta of={DeveloperStories} />

# Getting Started as a Developer

<p className="lead">Welcome to the Swiss Post Design System world!</p>

## Which package do I need?

The design-system is formed with several packages and different compatibilities with your favorite framework.

<div className="table-responsive" style={{'--post-docs-tile-size': '100px', '--post-docs-tile-rg-size': '100px'}}>
  <table className="table table-bordered">
    <caption className="visually-hidden">Compatibilities between components and frameworks and which package is needed</caption>
    <colgroup>
      <col style={{width: '25%'}}/>
      <col style={{width: '15%'}}/>
      <col style={{width: '20%'}}/>
      <col style={{width: '20%'}}/>
      <col style={{width: '20%'}} />
    </colgroup>
    <thead>
      <tr>
        <th scope="col">Component</th>
        <th scope="col">Base + …</th>
        <th scope="col">JavaScript (Vanilla)</th>
        <th scope="col">Angular</th>
        <th scope="col">React</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td scope="row">
          Internet-header:
          <list-component packageType={PackageType.InternetHeader}></list-component>
        </td>
        <td rowSpan="5">
          {[packages.find(pkg => pkg.name === 'Styles')].map((p => (
            <ti-le title={p.name} href={p.link.docs.href} ariaLabel={p.link.docs.ariaLabel} key={p.name}>
              <img src={p.img.src} alt={p.img.alt}/>
              <span>{p.version}</span>
            </ti-le>
          )))}
        </td>
        <td colSpan="2">
          <div style={{ display:'flex', justifyContent: 'center' }}>
          {[packages.find(pkg => pkg.name === 'Internet-Header')].map((p => (
            <ti-le title={p.name} href={p.link.docs.href} ariaLabel={p.link.docs.ariaLabel} key={p.name}>
              <img src={p.img.src} alt={p.img.alt}/>
              <span>{p.version}</span>
            </ti-le>
          )))}
          </div>
        </td>
        <td>TBD</td>
      </tr>
      <tr>
        <td scope="row">Intranet-header</td>
        <td>N/A</td>
        <td>
          {[packages.find(pkg => pkg.name === 'Intranet-Header')].map((p => (
            <ti-le title={p.name} href={p.link.docs.href} ariaLabel={p.link.docs.ariaLabel} key={p.name}>
              <img src={p.img.src} alt={p.img.alt}/>
              <span>{p.version}</span>
            </ti-le>
            )))}
        </td>
        <td >N/A</td>
      </tr>
      <tr>
        <td scope="row">
          Web Components:
          <list-component packageType={PackageType.Wc}></list-component>
        </td>
        <td>
          {[packages.find(pkg => pkg.name === 'Components')].map((p => (
            <ti-le title={p.name} href={p.link.docs.href} ariaLabel={p.link.docs.ariaLabel} key={p.name}>
              <img src={p.img.src} alt={p.img.alt}/>
              <span>{p.version}</span>
            </ti-le>
          )))}
        </td>
        <td>
          {[packages.find(pkg => pkg.name === 'Components for Angular')].map((p => (
            <ti-le title={p.name} href={p.link.docs.href} ariaLabel={p.link.docs.ariaLabel} key={p.name}>
              <img src={p.img.src} alt={p.img.alt}/>
              <span>{p.version}</span>
            </ti-le>
          )))}
        </td>
        <td>TBD</td>
      </tr>
      <tr>
        <td scope="row">
          Angular components:
          <list-component packageType={PackageType.Angular}></list-component>
        </td>
        <td>N/A</td>
        <td>
          <a href="https://ng-bootstrap.github.io">Ng-Bootstrap</a> module specific to the component
        </td>
        <td >N/A</td>
      </tr>
      <tr>
        <td scope="row">
          HTML components:
          <list-component packageType={PackageType.Html}></list-component>
        </td>
        <td colSpan="3" >
          No additional package is required
        </td>
      </tr>
    </tbody>
  </table>
</div>
