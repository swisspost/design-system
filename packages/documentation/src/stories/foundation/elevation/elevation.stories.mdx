import { Meta } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { ColorSwitch, ColorSwitchClass, BackgroundSwitch } from './elevation.components.tsx';
import './elevation.styles.scss';

<Meta
  title="Foundations/Elevation"
  parameters={{
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
    viewMode: 'docs',
    badges: [BADGE.NEEDS_REVISION],
  }}
/>

# Elevation

<div class="d-flex justify-content-between align-items-center">
  <ColorSwitchClass />
</div>
<BackgroundSwitch />

<div className="elevation-container">
  {[1, 2, 3, 4, 5].map(i => (
    <div className={`elevation-tile elevation-${i}`}>
      <h2 className="h6">Elevation {i}</h2>
      <p>
        <dl>
          <dt>Class</dt>
          <dd>
            <code>elevation-{i}</code>
          </dd>
        </dl>
      </p>
    </div>
  ))}
  {[1, 2, 3, 4, 5].map(i => (
    <div className={`elevation-tile elevation-${5 + i}`}>
      <h2 className="h6">Colored elevation {i}</h2>
      <p>
        <dl>
          <dt>Class</dt>
          <dd>
            <code>elevation-{5 + i}</code>
          </dd>
        </dl>
      </p>
    </div>
  ))}
</div>
