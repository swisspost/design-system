import { Meta } from '@storybook/blocks';
import LinkTo from '@storybook/addon-links/react';
import { forEach } from '../../../utils/react';
import * as ColorStories from './color.stories';
import { ColorSwatch, SCSS_VARIABLES } from './color.blocks';
import './color.styles.scss';

<Meta of={ColorStories} />

# Color

In the following, you can see the Swiss Post specific colors.<br/>
They theme our elements and components.

<div className="alert alert-info alert-md mt-regular">
  For accessibility reasons, you should always make sure that your individually created color combinations are sufficiently contrasting. There may be a better solution than coloring elements individually.

For example, use the <LinkTo kind="utilities-background" story="docs">background utilities</LinkTo> to get elements with a specific background color and the best possible contrast color for its content.

</div>

## Brand colors

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.brand, function (data) {
    return <ColorSwatch key={data.key} name={data.key} color={data.value} />;
  })}
</section>

## Shades of gray

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.gray, function (data) {
    return <ColorSwatch key={data.key} name={data.key} color={data.value} />;
  })}
</section>

## Page Backgrounds

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.pagebackgrounds, function (data) {
    return <ColorSwatch key={data.key} name={data.key} color={data.value} noCSS />;
  })}
</section>

## Signal colors

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.contextual, function (data) {
    return <ColorSwatch key={data.key} name={data.key} color={data.value} />;
  })}
</section>

## Dark accent colors

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.accent.dark, function (data) {
    return <ColorSwatch key={data.key} name={data.key} color={data.value} />;
  })}
</section>

## Bright accent colors

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.accent.bright, function (data) {
    return <ColorSwatch key={data.key} name={data.key} color={data.value} />;
  })}
</section>

# Usage

## CSS

CSS variables are globally available on your page.

```css dark
.my-element {
  property: var(--post-dark);
}
```

## Sass

Import the color variables from the `core` file with `@use`. This way, the color variables are scoped and don't mix with your own variables.

```scss dark
@use '@swisspost/design-system-styles/core' as post;

.my-element {
  property: post.$dark;
}
```
