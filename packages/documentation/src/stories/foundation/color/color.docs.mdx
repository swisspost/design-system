import { Meta, Source } from '@storybook/blocks';
import {
  PostTabHeader,
  PostTabPanel,
  PostTabs,
  PostBanner,
} from '@swisspost/design-system-components-react';
import { forEach } from '@/utils/react';
import * as ColorStories from './color.stories';
import { ColorSwatch, SCSS_VARIABLES } from './color.blocks';
import './color.styles.scss';
import SampleCss from './color-use-css.sample.css?raw';
import SampleScss from './color-use-scss.sample.scss?raw';

<Meta of={ColorStories} />

# Color

In the following, you can see the Swiss Post specific colors.<br/>
They theme our elements and components.

<PostBanner type="info">
  <p slot="heading">Notes for Accessibility</p>

For accessibility reasons, you should always make sure that your individually created color combinations are
sufficiently contrasting. There may be a better solution than coloring elements individually.

For example, use the [background utilities](/?path=/docs/60852fac-a861-4415-8276-bd38d68653bb--docs) to get elements
with a specific background color and the best possible contrast color for its content.

</PostBanner>

## Brand colors

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.brand, function ({ key, value }) {
    return <ColorSwatch key={key} name={key} color={value} />;
  })}
</section>

## Shades of gray

These colors are also available via the **Sass map** `$post-grays`.

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.gray, function ({ key, value }) {
    return <ColorSwatch key={key} name={key} color={value} />;
  })}
</section>

## Signal colors

These colors are also available via the **Sass map** `$signal-colors`.

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.signal, function ({ key, value }) {
    return <ColorSwatch key={key} name={key} color={value} />;
  })}
</section>

## Signal background colors

These colors are also available via the **Sass map** `$signal-background-colors`.

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES['signal-background'], function ({ key, value }) {
    return <ColorSwatch key={key} name={key} color={value} />;
  })}
</section>

## Black alpha colors

These colors are also available via the **Sass map** `$post-black-alphas`.

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES['black-alpha'], function ({ key, value }) {
    return <ColorSwatch key={key} name={key} color={value} />;
  })}
</section>

## White alpha colors

These colors are also available via the **Sass map** `$post-white-alphas`.

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES['white-alpha'], function ({ key, value }) {
    return <ColorSwatch key={key} name={key} color={value} dark />;
  })}
</section>

## Focus colors

<section className="color-swatch__section">
  {forEach(SCSS_VARIABLES.focus, function ({ key, value }) {
    return <ColorSwatch key={key} name={key} color={value} noCSS />;
  })}
</section>

# Usage

<PostTabs>
  <PostTabHeader panel="css">CSS</PostTabHeader>
  <PostTabPanel name="css">
    CSS variables are globally available on your page.

    <Source  code={SampleCss}  language="css"/>

  </PostTabPanel>

  <PostTabHeader panel="sass">Sass</PostTabHeader>
  <PostTabPanel name="sass">
    Import the color variables from the `core` file with `@use`. This way, the color variables are scoped and don't mix with your own variables.

    <Source  code={SampleScss}  language="scss"/>

  </PostTabPanel>
</PostTabs>
