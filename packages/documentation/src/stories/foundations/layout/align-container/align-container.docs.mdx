import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as alignContainerStories from './align-container.stories';

<Meta of={alignContainerStories} />

# Align Elements To Container

The `align-container-start` and `align-container-end` classes position content at the start and end of the container, respectively, ensuring the image extends to its boundaries.

When used together, they allow the element to span the full width, stretching from edge to edge.

## Basic Example

<Canvas sourceState="shown" of={alignContainerStories.Default} />
<Controls of={alignContainerStories.Default} />

## Grid Examples

<Canvas sourceState="shown" of={alignContainerStories.AlignContainerGrid} />
<Controls of={alignContainerStories.AlignContainerGrid} />
