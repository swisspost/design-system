import { Meta, Canvas, Controls, Source } from '@storybook/blocks';
import * as HeaderWithCustomLogoLink from './header-with-custom-logo-link.stories';

<Meta of={HeaderWithCustomLogoLink} />

# Implementing Post Header with custom logo link routing

For client-side routing applications, such as those built with Angular, React, Next.js, etc. the `<post-header>` logo component, `<post-logo>`, can be adapted to adhere to the specific implementation and utilize the respective routing mechanisms. Below is an example of how to integrate the header logo with custom routing in various frameworks.

## Post Logo Example

<Canvas of={HeaderWithCustomLogoLink.Default} sourceState="shown" />
<Controls of={HeaderWithCustomLogoLink.Default} />
<Source of={HeaderWithCustomLogoLink.Default} language="html" />

## Complete Header Example

<Canvas of={HeaderWithCustomLogoLink.Default} sourceState="shown" />
<Controls of={HeaderWithCustomLogoLink.Default} />
