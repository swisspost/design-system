import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as TooltipStories from './tooltip.stories';
import LinkTo from '@storybook/addon-links/react';

<Meta of={TooltipStories} />

# Tooltip

<div className="lead">For short, informative messages or explanations.</div>

<Canvas of={TooltipStories.Default} />
<Controls of={TooltipStories.Default} />

## Installation

The `<post-tooltip>` element is part of the `@swisspost/design-system-components` package. For more information, read the <LinkTo kind="getting-started-components" story="docs">getting started with components guide</LinkTo>.
