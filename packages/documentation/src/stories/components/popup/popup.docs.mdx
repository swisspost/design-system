import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as PopupStories from './popup.stories';
import LinkTo from '@storybook/addon-links/react';

<Meta of={PopupStories} />

# Popup

<div className="lead">
  For longer, informative messages or explanations and also simple interactive elements like links.
</div>

<Canvas of={PopupStories.Default} />
<Controls of={PopupStories.Default} />

## Installation

<div>
  The `<post-popup>` element is part of the `@swisspost/design-system-components` package. For more information, read the <LinkTo kind="getting-started-components" story="docs">getting started with components guide</LinkTo>.
</div>
