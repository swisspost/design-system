import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import * as SelectionCardStories from './selection-card.stories';
import StylesPackageImport from '@/shared/styles-package-import';

<Meta of={SelectionCardStories} />

<div className="docs-title">
  # Selection card

<link-design of={JSON.stringify(SelectionCardStories)}></link-design>

</div>

<p className="lead">For a more specialized visualization of checkbox and radio elements.</p>

<Canvas sourceState="shown" of={SelectionCardStories.Default} />
<div className="hide-col-default">
  <Controls of={SelectionCardStories.Default} />
</div>

## Examples

### Custom icon

You can use our built-in icons by using our `post-icon` component.
If this is not sufficient, you can also use a custom icon by providing a `<svg>` or `<img>` element.
See how this can be achieved in the following example.

<Canvas of={SelectionCardStories.CustomIcon} />
<div className="hide-col-default">
  <Controls of={SelectionCardStories.CustomIcon} />
</div>

### Custom content

If you need to add additional content to the component, you can do that as shown in the example below.

<post-banner type="warning">
  <p slot="heading">Even if it is generally possible to add other interactive elements within the component, this is strongly discouraged.</p>
  <p>It can cause confusion when the hit area of nested interactive elements is not clearly separated from the component, may result in invalid HTML, and event bubbling can lead to unexpected behavior.<br />More info: <a href="https://accessibilityinsights.io/info-examples/web/nested-interactive/">https://accessibilityinsights.io/info-examples/web/nested-interactive/</a></p>
</post-banner>

<Canvas of={SelectionCardStories.CustomContent} />

### Grouping

Multiple instances of the component can be grouped together using a `<fieldset>` and `<legend>` element to properly label the group.
Group validation differs slightly from individual validation. For more information, read our [form validation docs](/?path=/docs/1aa900d9-aa65-4ae0-b8cd-e6cca6cc3472--docs#radio-button-group).

<Canvas of={SelectionCardStories.Grouping} />
<div className="hide-col-default">
  <Controls of={SelectionCardStories.Grouping} />
</div>

### Lined up

If you use this component multiple times in a grid and some instances contain more content than others, their heights will differ.
To align them, simply add the class `.h-full` to all of them.

<Canvas of={SelectionCardStories.Lineup} />

<StylesPackageImport components={['choice-control-card']} />
