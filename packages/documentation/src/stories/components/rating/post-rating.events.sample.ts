const rating = document.querySelector('post-rating') as HTMLPostRatingElement;

rating.addEventListener('input', event => {
  // use event.detail.value for what ever you like
});

rating.addEventListener('change', event => {
  // use event.detail.value for what ever you like
});
