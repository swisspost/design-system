import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import * as linkareaStories from './linkarea.stories';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={linkareaStories} />

# Link Area

<p className="lead">A web component that delegates clicks on its area.</p>

The <code>post-linkarea</code> component allows only one interactive element to avoid user confusion over which link would be activated on click.

<Canvas sourceState="shown" of={linkareaStories.Default} />
<div className="hide-col-default">
  <Controls of={linkareaStories.Default} />
</div>

<ComponentsPackageInstall component="<post-linkarea>" />
