import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as SearchInputStories from './search.stories';
import StylesPackageImport from '@/shared/styles-package-import';
import PackageTag from '@/shared/package-tag';

<Meta of={SearchInputStories} />

<div className="docs-title">
  # Search Input

<link-design of={JSON.stringify(SearchInputStories)}></link-design>

</div>

<PackageTag meta={meta} />

<div className="lead">
  Customize the native `<input type="search">` element with CSS that changes the element’s initial appearance.
</div>

<Canvas sourceState="shown" of={SearchInputStories.Default} />

<div className="hide-col-default">
  <Controls of={SearchInputStories.Default} />
</div>

<StylesPackageImport
  components={['form-search-input', 'form-input']}
  required={{ validation: true }}
/>
