import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as StepperStories from './stepper.stories';
import PackageTag from '@/shared/package-tag';

<Meta of={StepperStories} />

<div className="docs-title">
  # Stepper

<link-design of={JSON.stringify(StepperStories)}></link-design>

</div>

<PackageTag meta={meta} />

<div className="lead mb-16">
  A stepper guides users through any linear, multistep process by showing the user their completed,
  current, and future steps in an interactive manner.
</div>

<Canvas sourceState="shown" of={StepperStories.Default} />
<div className="hide-col-default">
  <Controls of={StepperStories.Default} />
</div>

## Selected step

The current step and the selected step represent two different concepts:

- The current step comes immediately after the last completed step. It is the next step the user should complete, but it may not be the currently selected step.
- The selected step is the "currently active" step that the user is on. It can be either a completed step or the current step.

Here is what a stepper looks like with different selected (in bold) and current (in black) steps:

<Canvas sourceState="hidden" of={StepperStories.Selected} />

## Installation

The `<post-stepper>` element is part of the `@swisspost/design-system-components` package.
For more information, read the [getting started with components guide](/?path=/docs/edfb619b-fda1-4570-bf25-20830303d483--docs).
