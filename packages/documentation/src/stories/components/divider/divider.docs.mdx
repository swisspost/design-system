import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks';
import meta, * as DividerStories from './divider.stories';
import StylesPackageImport from '@/shared/styles-package-import.mdx';
import PackageTag from '@/shared/package-tag';

<Meta of={DividerStories} />

<div className="docs-title">
  # Divider

  <link-design of={JSON.stringify(DividerStories)}></link-design>
</div>

<PackageTag meta={meta}/>

<p className="lead">The Divider is a horizontal line to visually separate or group content to increase readability and understandability of a page.</p>
<p>Dividers can be used in various contexts, for example within forms, lists or sections. The `<hr>` HTML tag represents a thematic break between paragraph elements.</p>

<Canvas sourceState="shown" of={DividerStories.Default} />
<div className="hide-col-default">
  <Controls of={DividerStories.Default} />
</div>

<StylesPackageImport components={['divider']} />

## Usage

Dividers are often used within forms.

## Examples

[Page simple form](https://www.figma.com/design/1U0tACfFegz2s1ThIr4M06/Layout-Examples-Next-Level?node-id=2003-13683&p=f&t=7gscIgkLxrKqNPyv-0).


## Accessibility

The fulfill the WCAG success criterion of 3:1 contrast against the background if used with their respective pre-defined backgrounds of the palettes.

[Understanding SC 1.4.11: Non-text Contrast](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html)
