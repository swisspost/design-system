import headingMeta, { Default } from './heading.stories';
import { bombArgs } from '../../utilities/bombArgs';

<Meta title="Hidden/Headings snapshot test" />

<div>
  {['bg-white', 'bg-dark'].map(bg => {
    return (
      <div
        class={bg + ' mt-3'}
        style={{
          padding: '1rem',
          display: 'flex',
          flexDirection: 'column',
          gap: '1rem',
        }}
      >
        {bombArgs({
          title: [
            'Lorem ipsum dolor sit',
            'Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptatibus, libero. Dolore possimus ut atque quaerat nobis iusto vero, reiciendis sapiente',
          ],
          level: headingMeta.argTypes.level.options,
          showSubtitle: [false, true],
          subtitle: [
            'Lorem ipsum dolor sit amet',
            'Lorem, ipsum dolor sit amet consectetur adipisicing elit. Quam veniam inventore nulla saepe veritatis ut',
          ],
        })
          .filter(args => !(!args.showSubtitle && args.subtitle.length > 30))
          .map(args => ({ ...args, title: `${args.level} - ${args.title}` }))
          .map(args => Default({ ...headingMeta.args, ...args }))}
      </div>
    );
  })}
</div>
