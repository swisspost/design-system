import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as InlineNotificationStories from './inline-notification.stories';
import StylesPackageImport from '@/shared/styles-package-import';
import PackageTag from '@/shared/package-tag';

<Meta of={InlineNotificationStories} />

<div className="docs-title">
  # Inline notification

<link-design of={JSON.stringify(InlineNotificationStories)}></link-design>

</div>

<PackageTag meta={meta} />

<div className="lead">
  Inline notifications show up in task flows, to communicate the users the status of an action or system. It can also be used as a persistent element on the page to communicate information which needs higher attention than regular text.
</div>

<Canvas of={InlineNotificationStories.Default} sourceState="shown" />
<div className="hide-col-default">
  <Controls of={InlineNotificationStories.Default} />
</div>

<StylesPackageImport components={['Inline Notifications']} />

## Color options

Here is a quick overview of all the background colors available for this component.
<Canvas of={InlineNotificationStories.Variants} />

## Multi-line

Use the multi-line layout when the message is longer or when a title is needed.
<Canvas of={InlineNotificationStories.MultiLine} />

## When to use

Use an inline notification when the message relates to the current task, requires no action on the user's part, and needs to remain visible at all times. It is also the appropriate choice when the user must take action and the decision is required to proceed, but is not part of the intended flow (for example, a validation error in a form).
