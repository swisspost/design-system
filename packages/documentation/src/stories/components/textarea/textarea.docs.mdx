import { Meta, Canvas, Story, Source, ArgsTable } from '@storybook/addon-docs';

<Meta title="Components/Textarea" />

# Textarea

<p class="lead">
  Customize the native <code>&lt;textarea&gt;</code> with CSS that changes the element’s initial
  appearance.
</p>

Custom `textarea` elements need only the class `form-control` to trigger the custom styles.

<Canvas>
  <Story id="components-textarea--default" />
</Canvas>
<ArgsTable story="^" />

## SCSS Imports

To import all styles from the design-system-styles package:

```css dark
@use "@swisspost/design-system-styles/index.scss";
```

To import only the styles required for this component:

```css dark
@use "@swisspost/design-system-styles/basis.scss";
@use '@swisspost/design-system-styles/components/forms.scss';

// required if you use floating-labels
@use '@swisspost/design-system-styles/components/floating-label.scss';

// required if you use validation feedbacks
@use '@swisspost/design-system-styles/components/form-feedback.scss';
```

## Specific examples of application

The following examples show the different characteristics of the component. These can differ in the type of visualization, the html structure, as well as when, how and why they are displayed.

### Floating Label

Wrap a pair of `textarea` and `label` elements in a `.form-floating` container to enable floating labels.<br/>
But note that the `textarea` must come first, so we can ensure the correct styles.

<Canvas>
  <Story id="components-textarea--floating-label" />
</Canvas>

### Sizing

The size can be changed by simply adding one of four classes:

- Small (`form-control-sm`)
- Regular (`form-control-rg`)
- Medium (default or `form-control-md`)
- Large (`form-control-lg`)

<p class="text-danger">
  Note: Do not use this classes on a <code>textarea</code> element wrapped within a
  <code>.floating-label</code> container.
</p>

<Canvas>
  <Story id="components-textarea--size" />
</Canvas>

### Rows

To set a custom height on your `textarea`, use the `rows` attribute.

<Canvas>
  <Story id="components-textarea--rows" />
</Canvas>

### Helper Text

Optional descriptive text which is always shown in a `.form-text` element.

<Canvas>
  <Story id="components-textarea--helper-text" />
</Canvas>

### Disabled

Add the disabled boolean attribute on a `textarea` to give it a grayed out appearance and remove pointer events.

<Canvas>
  <Story id="components-textarea--disabled" />
</Canvas>

### Validation

Simply add the classes `is-valid` or `is-invalid` to the `textarea` element to show it in the expected state.<br/>
If you like to have some text as feedback, instead of only colors and icons, you can add `.valid-feedback` and `.invalid-feedback` elements after the `textarea`.

<Canvas>
  <Story id="components-textarea--validation" />
</Canvas>
