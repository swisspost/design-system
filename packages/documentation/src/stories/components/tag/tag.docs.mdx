import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as TagStories from './tag.stories';
import StylesPackageImport from '@/shared/styles-package-import';
import PackageTag from '@/shared/package-tag';

<Meta of={TagStories} />

<div className="docs-title">
  # Tag

<link-design of={JSON.stringify(TagStories)}></link-design>

</div>

<PackageTag meta={meta} />

<div className="lead">
  A small label, generally appearing inside or in close proximity to another larger interface
  component, representing a status, property, or some other metadata.
</div>

<Canvas of={TagStories.Default} sourceState="shown" />
<div className="hide-col-default">
  <Controls of={TagStories.Default} />
</div>

<StylesPackageImport components={['tag']} />

## Neutral tag

You can add any icon to the neutral tag component.
Find the icon you need with the <a href="/?path=/docs/0dcfe3c0-bfc0-4107-b43b-7e9d825b805f--docs">icon search page</a>.

<Canvas of={TagStories.Neutral} />

## Signal tags

Here is a quick overview of all the signal tags.
These tags have a signal icon by default that can be overriden by any other icon.

<Canvas of={TagStories.Variants} />
