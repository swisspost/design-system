import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
import meta, * as PopoverStories from './popover.stories';
import PackageTag from '@/shared/package-tag';
import ComponentsPackageInstall from '@/shared/components-package-install.mdx';

<Meta of={PopoverStories} />

<div className="docs-title">
  # Popover

<link-design of={JSON.stringify(PopoverStories)}></link-design>

</div>

<PackageTag meta={meta} />

<div className="lead">
  For longer, informative messages or explanations and also simple interactive elements like links.
</div>

<Canvas sourceState="shown" of={PopoverStories.Default} />
<Controls of={PopoverStories.Default} />

## Wrapped Popover

The `<post-popover>` can be nested within the `<post-popover-trigger>` to establish the trigger–popover relationship, eliminating the need for an explicit id reference.

<Canvas sourceState="shown" of={PopoverStories.Wrapped} />

<ComponentsPackageInstall component="<post-popover>" />
