import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as PopoverStories from './popover.stories';

<Meta of={PopoverStories} />

<div className="docs-title">
  # Popover

  <link-design of={JSON.stringify(PopoverStories)}></link-design>
</div>

<div className="lead">
  For longer, informative messages or explanations and also simple interactive elements like links.
</div>

<Canvas sourceState="shown" of={PopoverStories.Default} />
<Controls of={PopoverStories.Default} />

## Installation

<div>
  The `<post-popover>` element is part of the `@swisspost/design-system-components` package. For more information, read the [getting started with components guide](/?path=/docs/edfb619b-fda1-4570-bf25-20830303d483--docs).
</div>
