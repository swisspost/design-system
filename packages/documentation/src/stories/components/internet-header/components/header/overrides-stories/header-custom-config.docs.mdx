import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as HeaderStories from './header-custom-config.stories';
import '../header.scss';

<Meta of={HeaderStories} />

<div className="container">
  <h1>Internet Header with Custom Config</h1>
  <p className="lead">When you need to add your own navigation entries.</p>
</div>

<Canvas of={HeaderStories.Default} />

<div className="container">
  <Controls of={HeaderStories.Default} />
</div>
