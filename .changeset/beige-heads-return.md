---
'@swisspost/design-system-components': minor
---

Updated the `<post-collapsible-trigger>` component to support nesting without id references. A `<post-collapsible>` can now be placed directly inside `<post-collapsible-trigger>` instead of using the `for` attribute.
