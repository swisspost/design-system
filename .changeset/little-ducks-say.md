---
'@swisspost/design-system-styles': major
---

Removed the `traget-group.css` file, target group styles are now bundled with the `post-header` styles.
The `.traget-group` class no longer exist and the component will only work when placed into a `post-header`.
