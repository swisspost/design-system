---
'@swisspost/internet-header': minor
---

Updated `swisspost-internet-header` to fetch config per-language via the `lang` parameter, instead of fetching all languages in one call and selecting client-side.
