---
'@swisspost/design-system-components': minor
---

Added a `home-text` property to `<post-breadcrumbs>` to display `text-home` as visible text instead of the home icon, enabling segment specific root links (e.g. "Private customers" instead of the generic home link).
