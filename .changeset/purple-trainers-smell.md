---
"@swisspost/design-system-components": patch
---

Fixed inconsistent flyout flipping on window resize for `post-popover`, `post-menu`, and `post-datepicker` when using `*-end` / `*-start` placement at 1.25 DPR.
