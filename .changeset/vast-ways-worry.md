---
"@swisspost/design-system-documentation": patch
---

chore(documentation): Removed the non-recommended `target="_blank"` from the `post-linkarea` component markup. Also removed the obsolete occurrences of `target="_self"` from the `skiplinks` component markup.
