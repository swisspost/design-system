---
'@swisspost/design-system-components': minor
---

Added a `home-text` property to `<post-breadcrumbs>`. Set it to `true` to display `text-home` visibly instead of the home icon, enabling segment specific breadcrumbs (e.g. "Private customers" or "About us" as the root segment instead of a generic home link).
