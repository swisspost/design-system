---
'@swisspost/design-system-styles': patch
---

Tokenized the `dialog` component: colors, border, border-radius, elevation, spacing, icon sizing and heading/content typography now use the dedicated `dialog-*` design tokens instead of hardcoded values.
