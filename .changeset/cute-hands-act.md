---
'@swisspost/design-system-styles': major
'@swisspost/design-system-documentation': patch
---

Removed `.inline-list` classes from lists within the `post-header` component.
Lists in `post-header` are now styled automatically, keeping the `.inline-list` class will cause incorrect spacing between header elements.
