---
'@swisspost/design-system-components': patch
---

Updated the `post-breadcrumb-item` component to use the provided `url` directly as the `href`.
Fixes an issue with incomplete URLs and aligns behavior with standard browser `href` handling.
