---
'@swisspost/internet-header': major
---

Removed the `swisspost-internet-breadcrumbs` component, as breadcrumbs are not included in the online service header. Use the self-managed `<post-breadcrumbs>` and `<post-breadcrumb-item>` components instead.
