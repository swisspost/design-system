---
'@swisspost/internet-header': major
---

Removed the `swisspost-internet-breadcrumbs` component, since the Header Configuration API no longer provides breadcrumb data. Use the self-managed `<post-breadcrumbs>` and `<post-breadcrumb-item>` components instead.
