---
'@swisspost/design-system-components': minor
---

Added the parts `::part(post-accordion-button)` and `::part(post-accordion-body)` in the `post-accordion-item` component, so one can override styles from the outside.
