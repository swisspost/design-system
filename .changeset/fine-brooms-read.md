---
'@swisspost/design-system-components': patch
---

Patched the `post-linkarea` web component, so it can not only be used for click event delegation on interactive cards, but also for the newly implemented `.card-control` HTML/CSS component.
